(ns pushup-graphs.pushup-graphs
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X pushup-graphs.pushup-graphs/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m pushup-graphs.pushup-graphs"
  [& args]
  (println "-main with" args))
